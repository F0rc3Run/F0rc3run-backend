name: Trigger Private Workflow

on:
  workflow_dispatch: # دستی اجرا میشه

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: 🔔 Trigger Private Repo Workflow
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github.everest-preview+json" \
            -H "Authorization: token ${{ secrets.PAT }}" \
            https://api.github.com/repos/F0rc3Run/link-submitter/dispatches \
            -d '{"event_type": "run-checker"}'
